version: "3"

services:
  prometheus:
    ports:
    - 9090:9090
    build:
      context: ..
      dockerfile: docker/prometheus.dockerfile
  dashboard:
    ports:
    - 3000:3000
    image: grafana/grafana:7.2.0
